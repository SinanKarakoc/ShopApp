@model List<CategoryEntity>
<div class="list-group">

    <a href="/product/ProductList" class="list-group-item list-group-item-action ">Tüm Kategoriler</a>
    @foreach (var item in Model)
    {
        <a href="/product/ProductList/@item.CategoryId"
            class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == item.CategoryId.ToString()?"active":"")">@item.Name</a>
    }
</div>